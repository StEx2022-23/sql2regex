<!-- first line == sample solution, follow line == equivalent solutions, five # as delimiter between statements --!>

CREATE DATABASE Wetterdaten;
CREATE DATABASE "Wetterdaten";
CREAT DATABAS Wetterdatn
#####
CREATE TABLE wetterstation (id integer NOT NULL, name varchar(255), PRIMARY KEY (id));
CREATE TABLE wetterstation (id integer NOT NULL, name varchar( 255 ), PRIMARY KEY ( id ));
CREATE TABLE wetterstation (id integer NOT NULL, name varchar ( 255 ), PRIMARY KEY ( id) );
CREATE TABLE wetterstation (name varchar(255), id integer NOT NULL, PRIMARY KEY (id));
CREATE TABLE wetterstation (id integer NOT NULL, name varchar(255), PRIMARY KEY (id));
CREATE TABLE wetterstation (id integer NOT NULL, PRIMARY KEY (id), name varchar(255));
CREATE TABLE wetterstation (id int NOT NULL, name varchar(255), PRIMARY KEY (id));
CREATE TABLE wetterstation (id int NOT NULL, name varchar(255), PRIMARYKEY (id));
CREATE TABLE wetterstation ( id integer NOT NULL, name varchar(255), PRIMARY KEY (id) );
CREATE TABLE wetterstation(id integer NOT NULL,name varchar(255),PRIMARY KEY (id));